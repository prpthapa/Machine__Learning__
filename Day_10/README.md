## Day 10 - DataFrame Functions and Sorting

Today I learned and practiced:

- DataFrame functions
- Using `value_counts()`
- Sorting with `sort_values()`
- Sorting by multiple columns
- Using `sort_index()`
- Setting and resetting index with `set_index()` and `reset_index()`

These concepts helped me improve data organization and analysis using Pandas.
