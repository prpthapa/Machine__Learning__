## Day 14 - Combining DataFrames

Today I learned and practiced:

- Merging DataFrames using `merge()`
- Joining DataFrames using `join()`
- Concatenating DataFrames using `concat()`
- Understanding different types of joins (inner, left, right, outer)

These concepts helped me understand how to combine multiple datasets efficiently for analysis.
